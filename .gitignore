# Builds (regenerables con firmware/tools/build_*.sh)
firmware/build-sim/
firmware/build-ipod6g/
firmware/build-ipod6g-boot/
firmware/toolchain/
firmware/test-media/

# Assets de distribución generados (package_dist.sh), salvo el README
firmware/dist/*
!firmware/dist/README.md

# macOS
.DS_Store
*.dSYM/

# venv usado por firmware/tools/gen_logo.py (Pillow) -- ver DECISIONS.md M-002
firmware/tools/.venv/

# Fuentes fuente vendoreadas (TTF de Selawik) — SÍ se versionan, no ignorar
!firmware/assets/fonts-src/

# Binarios de los tests host-side (make -C firmware/rockbox/apps/metro/test)
firmware/rockbox/apps/metro/test/build/

# venv del sistema de diseño (design-system/generate.py) y su salida no versionada
design-system/.venv/
design-system/out/
